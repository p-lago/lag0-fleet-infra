apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-mysql-pvc
  labels:
    app: nextcloud-mysql
spec:
  storageClassName: nfs-csi
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi